spring:
  profiles:
    active: test

app:
  name: Todo List Manager
  version: 3.0.0
  api-version: 2.0.0
  security:
    pepper: test-pepper
    jwt:
      secret: test-jwt-secret-key-minimum-32-characters
      expiration-seconds: 3600
